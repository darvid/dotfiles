# code: language=shellscript
if [[ -n $SSH_CONNECTION ]]; then
  export EDITOR="vim"
else
  export EDITOR="code -w"
fi
export BROWSER=explorer

export LC_ALL="en_US.UTF-8"
export LANG="en_US"

export AWS_VAULT_BACKEND=file
export AWS_VAULT_PL_BROWSER=google-chrome
